# 値はローカルの .env に置く。このファイルは名前と開発用の例だけ。
# Git に実キーや本番接続先を書かない。

DATABASE_URL=ecto://postgres:postgres@db:5432/docker_bitflyer_dev
SECRET_KEY_BASE=change_me_generate_with_mix_phx_gen_secret
PHX_HOST=localhost
# dry_run（既定）: 出さない・擬似約定しない。paper: 擬似約定。live: 実発注。
TRADE_MODE=dry_run

